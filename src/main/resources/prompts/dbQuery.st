Given the schema in the SCHEMA section, construct a query that answers the question.  The query should
NOT update the database, it should only construct a query that reads the data. Any question that attempts
to update the database should return an empty query.

{prompt}

SCHEMA:
{schema}
